// ./ui/theme

import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#802A1E" },
  secondary: { main: "#331310" },
};
const themeName = "Red Robin Coffee Bean Hyrax";

export default createMuiTheme({ palette, themeName });
